import React from 'react';
import { render } from 'react-native-testing-library';
import ArrowIcon from './ArrowIcon';

describe('ArrowIcon styled component', () => {
  test('should render', async () => {
    render(<ArrowIcon />);
  });
});
