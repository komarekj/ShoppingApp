import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontWeight: '700',
    textAlign: 'center',
    marginBottom: 20,
    marginTop: 20,
  },
  topButton: {
    marginBottom: 20,
  },
  bottomButton: {
    marginTop: 20,
  },
});

export default styles;
