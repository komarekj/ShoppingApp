import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  section: {
    marginTop: 40,
    marginBottom: 45,
  },
  title: {
    textAlign: 'center',
    marginBottom: 8,
    fontWeight: '600',
    fontSize: 20,
  },
});

export default styles;
