import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrap: {
    marginBottom: 25,
  },
  pagination: {
    paddingTop: 5,
    paddingBottom: 0,
  },
});

export default styles;
