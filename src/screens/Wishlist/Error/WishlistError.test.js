import React from 'react';
import { render } from 'react-native-testing-library';
import WishlistError from './WishlistError';

describe('WishlistError screen component', () => {
  test('should render', () => {
    render(<WishlistError />);
  });
});
