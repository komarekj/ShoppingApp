import React from 'react';
import { render } from 'react-native-testing-library';
import CollectionError from './CollectionError';

describe('CollectionError screen component', () => {
  test('should render', () => {
    render(<CollectionError />);
  });
});
