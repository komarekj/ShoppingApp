import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginBottom: 30,
  },
  formWrap: {
    flexBasis: '100%',
  },
  title: {
    fontWeight: '700',
    marginBottom: 5,
    fontSize: 14,
  },
});

export default styles;
